---
name: Udex secret-handling pattern (ST0008)
description: How Udex handles secrets across config, CLI, datastore, and dev tooling — the load-bearing decisions to apply when reviewing security-sensitive code
type: project
---

ST0008 (steel thread "inject keys and secrets") established the canonical secret model.

**Why:** Several private keys and hardcoded passwords were originally tracked in git.
ST0008 removes them and codifies a single injection mechanism that works in dev, CI,
and prod with no per-environment branching.

**How to apply when reviewing Rust code:**
- All string secrets must be typed `secrets_rs::Secret<T>` in config structs.
  `Secret<T>` masks the value in `Debug`/`Display`/serde and forces explicit `.value()`
  access. The type itself is the audit signal — there is no `_secret` field-name
  convention (it was explicitly rejected in WP-04 — see `intent/st/ST0008/design.md`).
- TOML config files reference secrets by URN: `urn:secrets-rs:env:VAR_NAME`. The loader
  calls `bind_all()` after deserialisation; missing env vars must be a hard startup error.
- `Secret<T>::Deserialize` is the file-injection guard: it only accepts a bare URN, so
  raw secret values in TOML fail at parse time. Do not add a separate guard layer.
- CLI flags must NOT accept secret values (they leak into `ps` output and shell history).
  Bearer tokens come from `UDEX_TOKEN`; OAuth2 client secret comes from `UDEX_CLIENT_SECRET`;
  `--token` and `--client-secret` flags must not exist on the CLI.
- Private key/cert files are generated by `scripts/gen-keys-and-certs.sh` and are
  gitignored. Public artefacts (`.crt`, public PEMs) are also gitignored and regenerated
  the same way.
- Any change to credentials, keys, or closely associated principals MUST update
  `SECRETS.md` in the same commit.
