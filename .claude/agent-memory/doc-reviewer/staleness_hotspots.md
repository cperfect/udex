---
name: staleness-hotspots
description: Udex doc facts that drift from code and where to verify them
metadata:
  type: reference
---

Recurring drift points in Udex docs — verify against source, don't trust prose:

- Version pins must agree across: `.devcontainer/Dockerfile` (RUST_VERSION 1.95.0), `.devcontainer/devcontainer.json` (protoc v34.1), `projects/compose/docker-compose.yml` (postgres:16, oryd/hydra:v26.2.0), `.devcontainer/post-create.sh` (HYDRA_VERSION). Docs referencing these: README, .devcontainer/README, projects/rust/CONTRIBUTING (canonical table), compose/README, k8s/README, server/README.
- datastore/README example uses postgres:13-alpine — contradicts the 16+ requirement (project standard). Likely-stale recommendation.
- "all three .proto files" wording in protobuf/README is stale — only 2 protos remain.
- Hydra-test skip behaviour: project rule (CLAUDE.md) = Hydra tests MUST NEVER skip; source `jwks_refresh_tests.rs` fails hard. server/README "skip gracefully" prose is stale. (Distinct from k8s tests which DO early-return when K8S_SERVER_URL unset — that's intended.)
- Test naming: canonical prefixes are layer indicators (test_sdk_, test_server_, test_index_service_, test_entry_service_, test_datastore_, test_cli_, +_oauth2_/_k8s_ variants). No `test_hydra_` prefix exists despite server/README claiming it.
- Generated-by HTML comments (`<!-- Generated by Claude Code ... -->`) appear atop several rust crate READMEs — AI artefact noise.
